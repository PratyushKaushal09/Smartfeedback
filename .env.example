# Public Vite envs (safe to commit if they are public)
# Replace with your Builder.io Public API key if available
VITE_PUBLIC_BUILDER_KEY=

# Private secrets (DO NOT COMMIT REAL VALUES)
# Set these in your shell/CI, not in .env checked into Git.
OPENROUTER_API_KEY=
